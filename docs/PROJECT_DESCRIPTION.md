# sysb-dashboard — Project Description

Date: 2026-05-07

## Project Overview

This repository implements the `sysb-dashboard` web application — a Next.js (app directory) TypeScript project providing a dashboard and API routes for lead management and authentication. The app includes UI components, API endpoints (server routes), and utilities for Supabase integration.

## Top-level Files

- `components.json`: Component metadata or registry for the UI components.
- `eslint.config.mjs`: ESLint configuration for the project.
- `next-env.d.ts`, `next.config.ts`, `tsconfig.json`: TypeScript and Next.js configuration files.
- `package.json`: Node dependencies and project scripts.
- `postcss.config.mjs`: PostCSS configuration for CSS processing.
- `proxy.ts`: Local proxy or helper for API routing or development.
- `README.md`: Project readme (entry documentation).
- `Restructuring.md`, `sprints.md`: Project planning and sprint notes.

## `app/` (Next.js app directory)

This folder contains the Next.js application using the `app` router structure.

- `globals.css`: Global stylesheet used across the app.
- `layout.tsx`: Application layout component used by Next.js to wrap pages.
- `page.tsx`: Root page for the app (likely landing/dashboard).

### `app/api/` (Server routes)

- `leads/confirm-company/route.ts`: API route to confirm a company for a lead.
- `leads/discover-company/route.ts`: API route to discover company information for a lead.
- `leads/network/route.ts`: API route exposing lead network information.
- `leads/research/route.ts`: API route for lead research operations.

### `app/auth/` (Auth routes)

- `callback/route.ts`: OAuth callback or auth provider callback route.
- `signout/route.ts`: Route handling sign-out.

### `app/leads/` (Pages and client components)

- `LeadsClient.tsx`: Client-side component rendering leads list or UI.
- `page.tsx`: Page that lists leads (uses `LeadsClient` or server data).
- `[id]/` (dynamic route for a specific lead):
  - `LeadDetailClient.tsx`: Client component showing lead details.
  - `LeadNetworkClient.tsx`: Client component to display network-related info for a lead.
  - `LeadStatusSelector.tsx`: UI component for changing a lead's status.
  - `page.tsx`: Server or client page wrapper for an individual lead.

### `app/login/`

- `page.tsx`: Login page for the application.

## `components/` (UI primitives and shared components)

This folder contains reusable UI components used throughout the app.

- `ui/accordion.tsx`: Accordion UI component.
- `ui/badge.tsx`: Badge UI component.
- `ui/button.tsx`: Button component(s) with styles and variants.
- `ui/card.tsx`: Card component for content grouping.
- `ui/input.tsx`: Input field component.
- `ui/select.tsx`: Select/dropdown component.
- `ui/separator.tsx`: Visual separator (divider) component.
- `ui/skeleton.tsx`: Skeleton loaders for async content.
- `ui/sonner.tsx`: Toast/notification wrapper (likely using Sonner library).
- `ui/table.tsx`: Table component used to display tabular data.
- `ui/tabs.tsx`: Tabs component for switching views.

## `lib/`

- `utils.ts`: Shared utility functions used throughout the project.

## `public/`

- Static assets served by Next.js (images, icons, etc.). The folder exists in the tree but contents are not listed here.

## `utils/supabase/`

- `client.ts`: Supabase client configuration for browser/client usage.
- `server.ts`: Supabase server/client credentials and helpers for server-side usage.

## Notes & Next Steps

- The `app/api` routes implement server logic—review those files for backend behavior and auth checks.
- UI components are colocated under `components/ui` and are small primitives intended for composition.
- SQL files at the root likely contain historic data restores or schema snapshots—review before running against any live DB.

If you'd like, I can:

- Expand each file description with important exported symbols and prop types.
- Generate a README-based quickstart with instructions for local development and environment variables.
- Add a table-of-contents or convert this into multiple docs files under `docs/`.

---
Generated by GitHub Copilot on 2026-05-07
